rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporarily allow all reads/writes for testing.
    // Make sure to secure this later!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
